apiVersion: v1
kind: Service
metadata:
  name: {{ include "lookout-app.fullname" . }}
  labels:
    {{- include "lookout-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "lookout-app.selectorLabels" . | nindent 4 }}
