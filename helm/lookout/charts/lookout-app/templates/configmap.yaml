apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lookout-app.fullname" . }}
  labels:
    {{- include "lookout-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
